<% if object.errors.any? %>
	<div class="alert alert-danger" id="error_explanation">
		<h2>
			<%= pluralize(object.errors.count, "error") %> prohibited this data from being saved:
			<ul>
				<% object.errors.full_messages.each do |msg| %>
				<li>
					<%= msg %>
				</li>
				<% end %>
			</ul>
		</h2>
	</div>
<% end %>